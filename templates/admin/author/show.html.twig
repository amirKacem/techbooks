{% extends 'admin/base.html.twig' %}

{% block title %}Author{% endblock %}

{% block body %}
    <h1>Author</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ author.id }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ author.firstName }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ author.lastName }}</td>
            </tr>
            <tr>
                <th>Full_name</th>
                <td>{{ author.fullName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_author_index') }}">All Authores</a>

    <a href="{{ path('admin_author_edit', {'id': author.id}) }}">edit</a>

    {{ include('admin/author/_delete_form.html.twig') }}
{% endblock %}
